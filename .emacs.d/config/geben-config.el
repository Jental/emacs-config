;; ------------------Geben----------------------

(setq load-path (cons "~/.emacs.d/elisp/geben/" load-path))
(autoload 'geben "geben" "DBGp protocol frontend, a script debugger" t)

(provide 'geben-config)
